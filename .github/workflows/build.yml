name: Build dylib

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install Theos
        run: |
          bash -c "$(curl -fsSL https://raw.githubusercontent.com/theos/theos/master/bin/install-theos)"

      - name: Build
        run: |
          export THEOS=~/theos
          make

      - name: Upload dylib
        uses: actions/upload-artifact@v3
        with:
          name: 8ball-pool.dylib
          path: .theos/obj/debug/8ball-pool.dylib

